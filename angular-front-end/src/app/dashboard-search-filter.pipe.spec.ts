import { DashboardSearchFilterPipe } from './dashboard-search-filter.pipe';

describe('DashboardSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
